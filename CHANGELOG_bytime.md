# Changelog (By Date & Time)

All notable changes to the LazyTasks Performance Scorer addon, grouped by date in chronological order.

---

## 2026-03-24

1. `13:20` — Initial commit: Performance Matrix addon v0.0.5 with scatter chart, PHP API, DB schema, React/Redux frontend *(Initial Release)*
2. `13:35` — Add CSS stylesheet and enqueue for performance dashboard *(Styling)*
3. `13:39` — Fix: hex colors for scatter bubble/badge backgrounds instead of CSS vars *(Styling)*
4. `13:50` — Fix: custom hover tooltip on scatter bubbles *(Dashboard)*
5. `14:20` — Fix: move `performance-scorer` permission to Setting group (global scope) *(Permissions)*
6. `17:36` — Refactor: `get_wpdb()` helper in DefaultController *(Code Quality)*
